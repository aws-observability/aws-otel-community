# Service account for Fluent Bit as a DaemonSet specifies an identity for processes in pod for authentication.
{{- if .Values.fluentbit.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.fluentbit.name }}
  namespace: {{ include "aws-for-fluent-bit.namespace" . }}
{{- end }}
