# Spring-boot integration tests sample-app

## Run
### Run as a command

Run this command in the directory `centralized-sampling-tests` to run the sample-app

```shell
./gradlew :sample-apps:spring-boot:run
```
